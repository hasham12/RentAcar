@model entity.CarMake

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CarMake</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.MakeNameId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MakeNameId)
            @Html.ValidationMessageFor(model => model.MakeNameId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.MakeName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MakeName)
            @Html.ValidationMessageFor(model => model.MakeName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


