@model IEnumerable<entity.CarMake>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MakeNameId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MakeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MakeNameId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MakeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.MakeNameId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MakeNameId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MakeNameId })
        </td>
    </tr>
}

</table>
